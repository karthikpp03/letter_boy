name = "letterboy-api"
main = "worker.js"
compatibility_date = "2024-01-01"

[vars]
# Set these as secrets instead:
# wrangler secret put TELEGRAM_BOT_TOKEN
# wrangler secret put TELEGRAM_CHAT_ID
# wrangler secret put GITHUB_TOKEN
# wrangler secret put GITHUB_REPO         (e.g. karthikpp03/letter_boy)
# wrangler secret put GITHUB_FILE_PATH    (e.g. letters.json)
# wrangler secret put ADMIN_KEY           (e.g. moon2024)
